{
    "general": {
        "autostart": true
    },
    "telemetry": {
        "considered": true, // For now we collect no telemetry, so the user does not have to opt-in
        "anonymousMetrics": true,
        "anonymousErrors": true,
        "nonAnonymousMetrics": false,
        "distinctId": null
    },
    "api": {
        "endpoint": "https://api.eurora-labs.com"
    }
}

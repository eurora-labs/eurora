[package]
name = "euro-transport-policy"
version = "0.0.0"
edition.workspace = true
license.workspace = true
description = "Named, documented timeouts and backoffs for every Eurora transport path (desktop⇄bridge, bridge⇄native host, chat stream)."

[dependencies]

[lints]
workspace = true

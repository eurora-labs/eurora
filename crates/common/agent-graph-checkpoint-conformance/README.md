# agent-graph-checkpoint-conformance

Conformance test suite for agent-graph checkpointer implementations.

This crate is a Rust rewrite of the `langgraph-checkpoint-conformance` Python library. It currently contains no public API — the name is reserved while the implementation is being developed.

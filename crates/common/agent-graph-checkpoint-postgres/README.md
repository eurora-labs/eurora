# agent-graph-checkpoint-postgres

Postgres implementation of an agent-graph checkpoint saver.

This crate is a Rust rewrite of the `langgraph-checkpoint-postgres` Python library. It currently contains no public API — the name is reserved while the implementation is being developed.

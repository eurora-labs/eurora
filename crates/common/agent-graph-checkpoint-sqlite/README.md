# agent-graph-checkpoint-sqlite

SQLite implementation of an agent-graph checkpoint saver.

This crate is a Rust rewrite of the `langgraph-checkpoint-sqlite` Python library. It currently contains no public API — the name is reserved while the implementation is being developed.

# agent-graph-checkpoint

Base interfaces for agent-graph checkpoint savers.

This crate is a Rust rewrite of the `langgraph-checkpoint` Python library. It currently contains no public API — the name is reserved while the implementation is being developed.

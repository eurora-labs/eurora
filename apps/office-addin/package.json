{
	"name": "@eurora/office-addin",
	"private": true,
	"version": "0.4.0",
	"type": "module",
	"description": "Eurora Microsoft Office add-in. Headless Shared Runtime that bridges Word into the Eurora desktop over a local WebSocket.",
	"scripts": {
		"dev": "vite",
		"build": "vite build && node scripts/copy-bundle-extras.mjs",
		"preview": "vite preview",
		"render-manifest": "node scripts/render-manifest.mjs",
		"ensure-certs": "office-addin-dev-certs verify || office-addin-dev-certs install",
		"start": "pnpm ensure-certs && pnpm render-manifest && office-addin-debugging start manifest.dev.xml desktop",
		"stop": "office-addin-debugging stop manifest.dev.xml",
		"validate": "pnpm render-manifest && office-addin-manifest validate manifest.dev.xml",
		"check-types": "tsc --noEmit",
		"lint": "prettier --check . && eslint .",
		"format": "prettier --write .",
		"test": "vitest run",
		"test:unit": "vitest"
	},
	"devDependencies": {
		"@types/node": "^22.20.1",
		"@types/office-js": "^1.0.599",
		"eslint": "^9.39.4",
		"office-addin-debugging": "^6.1.2",
		"office-addin-dev-certs": "^2.0.10",
		"office-addin-manifest": "^2.1.6",
		"prettier": "^3.9.4",
		"typescript": "5.9.3",
		"vite": "catalog:",
		"vitest": "catalog:"
	}
}
